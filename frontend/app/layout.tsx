import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
    variable: "--font-geist-sans",
    subsets: ["latin"],
});

const geistMono = Geist_Mono({
    variable: "--font-geist-mono",
    subsets: ["latin"],
});

export const metadata: Metadata = {
    title: "Bug Reproducer — Autonomous Bug Fixing Agent",
    description:
        "Paste a GitHub issue URL. An AI agent automatically reproduces the bug, writes a failing test, generates a fix, and opens a Pull Request.",
    openGraph: {
        title: "Bug Reproducer",
        description: "Autonomous bug reproduction and fixing with AI.",
    },
};

export default function RootLayout({
    children,
}: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <html
            lang="en"
            className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
        >
            <body className="min-h-full flex flex-col">{children}</body>
        </html>
    );
}
